import styled from 'styled-components';

const Container = styled.nav`
  width: 100%;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: space-around;
`;

export { Container };
